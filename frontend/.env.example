# Frontend environment variables
# Copy this file to `.env` and fill in real values.

# Base URL of the backend API host (WITHOUT the `/api` suffix).
# Next.js rewrites proxy `/api/*` to this host at build time.
# NOT exposed to the browser; set in the Vercel project env for production.
API_PROXY_TARGET="http://localhost:5000"